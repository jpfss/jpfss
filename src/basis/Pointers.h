#ifndef POINTERS_H
#define POINTERS_H

class Pointers {
public:
  Pointers();
  virtual ~Pointers();
  Pointers(const Pointers &other);
  Pointers &operator=(const Pointers &other);
  void go();

protected:
private:
};

#endif // POINTERS_H
