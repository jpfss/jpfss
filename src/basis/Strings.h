#ifndef STRINGS_H
#define STRINGS_H

class Strings {
public:
  Strings();
  virtual ~Strings();
  Strings(const Strings &other);
  Strings &operator=(const Strings &other);
  void go();

protected:
private:
};

#endif // STRINGS_H
